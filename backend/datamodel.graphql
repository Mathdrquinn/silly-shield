type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Tournament {
  id: ID! @unique,
  name: String!
  startDate: DateTime!
  playerCountLimit: Int!
  # roundDuration: Number!
  # RoundBuffer: Number!
  # type: String
  players: [User!]!
}
